# A2Z Sheets

Disclaimer: For educational purposes only.

## Tracks

- `/` — Hub
- `/dsa` — A2Z DSA problem tracker (IndexedDB progress)
- `/lld` — Java → OOP → SOLID → Patterns → LLD curriculum

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

GitHub Pages uses `base: /a2z-sheet/` and copies `index.html` → `404.html` for SPA deep links.
