{
  "version": 2,
  "id": "chckifllisplindrmrnt",
  "title": "Check if LL is palindrome or not",
  "topicNumber": 141,
  "stepTitle": "Learn LinkedList [Single LL, Double LL, Medium, Hard Problems]",
  "subStepTitle": "Medium Problems of LL",
  "category": "Learn LinkedList [Single LL, Double LL, Medium, Hard Problems]",
  "subcategory": "Medium Problems of LL",
  "difficulty": "medium",
  "topicType": "math",
  "sourceUrl": "https://takeuforward.org/data-structure/check-if-given-linked-list-is-plaindrome/",
  "quality": "reviewed",
  "tags": [
    "linked-list",
    "two-pointers"
  ],
  "pattern": "find middle + reverse half",
  "problemStatement": "Check whether a linked list is a palindrome in O(1) extra space.",
  "example": "1->2->2->1 -> True",
  "intuition": "You cannot walk backwards, so reverse the second half in place and compare it against the first half. Restoring the list afterwards is good manners and sometimes required.",
  "approaches": [
    {
      "title": "Brute: copy to an array",
      "idea": "Compare with two pointers on the array.",
      "code": "def is_palindrome_array(head) -> bool:\n    vals = []\n    cur = head\n    while cur:\n        vals.append(cur.val)\n        cur = cur.next\n    return vals == vals[::-1]",
      "language": "python",
      "time": "O(n)",
      "space": "O(n)",
      "flags": [
        "MLE"
      ]
    },
    {
      "title": "Optimal: reverse the second half",
      "idea": "Middle via slow/fast, reverse, compare, then restore.",
      "code": "def is_palindrome(head) -> bool:\n    if head is None or head.next is None:\n        return True\n    slow = fast = head\n    while fast.next and fast.next.next:\n        slow = slow.next\n        fast = fast.next.next\n\n    # reverse the second half\n    prev = None\n    curr = slow.next\n    while curr:\n        nxt = curr.next\n        curr.next = prev\n        prev = curr\n        curr = nxt\n    slow.next = prev\n\n    first, second = head, slow.next\n    ok = True\n    while second:\n        if first.val != second.val:\n            ok = False\n            break\n        first = first.next\n        second = second.next\n\n    # restore\n    curr = slow.next\n    prev = None\n    while curr:\n        nxt = curr.next\n        curr.next = prev\n        prev = curr\n        curr = nxt\n    slow.next = prev\n    return ok",
      "language": "python",
      "time": "O(n)",
      "space": "O(1) auxiliary",
      "dryRun": "1->2->2->1\nmiddle at first 2; reverse tail -> 1->2 | 1->2\ncompare 1==1, 2==2 -> True",
      "whyWorks": "Reversing makes the second half traversable in the order needed for comparison."
    }
  ],
  "complexity": {
    "time": "O(n)",
    "space": "O(1) auxiliary"
  },
  "edgeCases": [
    "Odd length — the middle element is skipped",
    "Empty or single node -> True",
    "Two nodes"
  ],
  "commonMistakes": [
    "Not restoring the list when the caller still needs it",
    "Off-by-one when locating the middle for odd lengths"
  ],
  "patternRecognition": [
    "Combine middle-finding and reversal — two primitives, one problem"
  ],
  "followUps": [
    "Do it without modifying the list (needs O(n) space or recursion)",
    "Palindrome check on a doubly linked list",
    "Reorder list using the same split-and-reverse"
  ],
  "interviewInsight": "Ask whether mutating the input is acceptable; the answer changes which solution is \"optimal\".",
  "related": [
    {
      "id": "lngthflpinll",
      "title": "Length of Loop in LL"
    },
    {
      "id": "sgrrgtddndvnndsinll",
      "title": "Segrregate odd and even nodes in LL"
    },
    {
      "id": "findthstrtingpintinll",
      "title": "Find the starting point in LL"
    }
  ]
}
