{
  "id": "jvcllctins",
  "title": "Java Collections",
  "stepTitle": "Learn the basics",
  "subStepTitle": "Learn STL/Java-Collections or similar thing in your\n            language",
  "sourceUrl": null,
  "intro": "C++ STL is leverage: vector, set, map, unordered_map, priority_queue, algorithms. Prefer clear containers over hand-rolled structures unless the problem needs it.",
  "intuition": "Choose the container by access pattern: vector for sequences, unordered_map for average lookups, set for ordered uniques.",
  "approach": [
    "Pick container from operations.",
    "Use algorithms (sort, lower_bound) instead of hand loops when clear.",
    "Mind iterator invalidation rules."
  ],
  "complexity": {
    "time": "Container-specific",
    "space": "Container-specific"
  },
  "pitfalls": [
    "Using map when unordered_map suffices (log factor).",
    "Iterator invalidation after insert/erase."
  ],
  "languages": {
    "cpp": {
      "code": "#include <bits/stdc++.h>\nusing namespace std;\n// Teach-yourself scaffold for: Java Collections\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int n; \n    if (!(cin >> n)) return 0;\n    vector<int> a(n);\n    for (int i = 0; i < n; i++) cin >> a[i];\n    // TODO: core idea for \"Java Collections\"\n    // 1) brute force for correctness\n    // 2) replace with the pattern from this step\n    cout << a.size() << \"\\n\";\n    return 0;\n}",
      "notes": "Fill the TODO with the approach above; keep IO fast."
    },
    "java": {
      "code": "import java.util.*;\npublic class Main {\n    // Teach-yourself scaffold for: Java Collections\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        if (!sc.hasNextInt()) return;\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        // TODO: implement \"Java Collections\"\n        System.out.println(a.length);\n    }\n}",
      "notes": "Replace the TODO; prefer clear names over micro-opts first."
    },
    "python": {
      "code": "# Teach-yourself scaffold for: Java Collections\nimport sys\n\ndef solve(a):\n    # TODO: core idea for \"Java Collections\"\n    return len(a)\n\ndef main():\n    data = list(map(int, sys.stdin.buffer.read().split()))\n    if not data: return\n    n, *rest = data\n    a = rest[:n]\n    print(solve(a))\n\nif __name__ == \"__main__\":\n    main()",
      "notes": "PyPy helps some TLs; still aim for the right complexity."
    },
    "go": {
      "code": "package main\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"os\"\n)\n// Teach-yourself scaffold for: Java Collections\nfunc solve(a []int) int {\n    // TODO: core idea for \"Java Collections\"\n    return len(a)\n}\nfunc main() {\n    in := bufio.NewReader(os.Stdin)\n    var n int\n    if _, err := fmt.Fscan(in, &n); err != nil { return }\n    a := make([]int, n)\n    for i := 0; i < n; i++ { fmt.Fscan(in, &a[i]) }\n    fmt.Println(solve(a))\n}",
      "notes": "Go rewards simple, explicit loops — good for interviews."
    }
  }
}
