{
  "id": "nmtingsinnrm",
  "title": "N meetings in one room",
  "stepTitle": "Greedy Algorithms [Easy, Medium/Hard]",
  "subStepTitle": "Medium/Hard",
  "sourceUrl": "https://takeuforward.org/data-structure/n-meetings-in-one-room/",
  "intro": "Greedy works when a local choice stays safe globally — prove exchange argument or use a known theorem.",
  "intuition": "Sort or pick extreme candidate; verify no better swap exists.",
  "approach": [
    "Sort by key.",
    "Take next safe candidate.",
    "Prove no better alternative exists."
  ],
  "complexity": {
    "time": "Usually O(n log n) from sort",
    "space": "O(1)–O(n)"
  },
  "pitfalls": [
    "No proof → WA on counterexamples.",
    "Sorting by the wrong key."
  ],
  "languages": {
    "cpp": {
      "code": "#include <bits/stdc++.h>\nusing namespace std;\n// Teach-yourself scaffold for: N meetings in one room\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int n; \n    if (!(cin >> n)) return 0;\n    vector<int> a(n);\n    for (int i = 0; i < n; i++) cin >> a[i];\n    // TODO: core idea for \"N meetings in one room\"\n    // 1) brute force for correctness\n    // 2) replace with the pattern from this step\n    cout << a.size() << \"\\n\";\n    return 0;\n}",
      "notes": "Fill the TODO with the approach above; keep IO fast."
    },
    "java": {
      "code": "import java.util.*;\npublic class Main {\n    // Teach-yourself scaffold for: N meetings in one room\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        if (!sc.hasNextInt()) return;\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        // TODO: implement \"N meetings in one room\"\n        System.out.println(a.length);\n    }\n}",
      "notes": "Replace the TODO; prefer clear names over micro-opts first."
    },
    "python": {
      "code": "# Teach-yourself scaffold for: N meetings in one room\nimport sys\n\ndef solve(a):\n    # TODO: core idea for \"N meetings in one room\"\n    return len(a)\n\ndef main():\n    data = list(map(int, sys.stdin.buffer.read().split()))\n    if not data: return\n    n, *rest = data\n    a = rest[:n]\n    print(solve(a))\n\nif __name__ == \"__main__\":\n    main()",
      "notes": "PyPy helps some TLs; still aim for the right complexity."
    },
    "go": {
      "code": "package main\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"os\"\n)\n// Teach-yourself scaffold for: N meetings in one room\nfunc solve(a []int) int {\n    // TODO: core idea for \"N meetings in one room\"\n    return len(a)\n}\nfunc main() {\n    in := bufio.NewReader(os.Stdin)\n    var n int\n    if _, err := fmt.Fscan(in, &n); err != nil { return }\n    a := make([]int, n)\n    for i := 0; i < n; i++ { fmt.Fscan(in, &a[i]) }\n    fmt.Println(solve(a))\n}",
      "notes": "Go rewards simple, explicit loops — good for interviews."
    }
  }
}
