{
  "version": 2,
  "id": "psclstringl",
  "title": "Pascal's Triangle",
  "topicNumber": 67,
  "stepTitle": "Solve Problems on Arrays [Easy -> Medium -> Hard]",
  "subStepTitle": "Hard",
  "category": "Solve Problems on Arrays [Easy -> Medium -> Hard]",
  "subcategory": "Hard",
  "difficulty": "medium",
  "topicType": "coding-problem",
  "sourceUrl": "https://takeuforward.org/data-structure/program-to-generate-pascals-triangle/",
  "quality": "reviewed",
  "tags": [
    "arrays",
    "combinatorics"
  ],
  "pattern": "binomial coefficients",
  "problemStatement": "Three variants: the value at (r, c), the whole r-th row, or the first n rows of Pascal's triangle.",
  "example": "row 5 -> [1,4,6,4,1]",
  "intuition": "Entry (r, c) is C(r-1, c-1). Building a row from the previous one is O(n^2) overall, but a single row can be generated in O(n) by the multiplicative identity C(n,k) = C(n,k-1) * (n-k+1)/k.",
  "approaches": [
    {
      "title": "Whole triangle: additive DP",
      "idea": "Each row is built from its neighbours above.",
      "code": "def pascal_triangle(n: int) -> list[list[int]]:\n    tri: list[list[int]] = []\n    for r in range(n):\n        row = [1] * (r + 1)\n        for c in range(1, r):\n            row[c] = tri[r - 1][c - 1] + tri[r - 1][c]\n        tri.append(row)\n    return tri",
      "language": "python",
      "time": "O(n^2)",
      "space": "O(n^2) for the output"
    },
    {
      "title": "Single row in O(n): multiplicative form",
      "idea": "Avoid factorials to keep intermediate values small.",
      "code": "def pascal_row(r: int) -> list[int]:\n    row = [1]\n    val = 1\n    for c in range(1, r):\n        val = val * (r - c) // c\n        row.append(val)\n    return row",
      "language": "python",
      "time": "O(r)",
      "space": "O(r)",
      "dryRun": "r=5\nval 1 -> 4 -> 6 -> 4 -> 1 giving [1,4,6,4,1]",
      "whyWorks": "C(n,k) = C(n,k-1) * (n-k+1)/k, and the division is always exact at each step."
    }
  ],
  "complexity": {
    "time": "O(n^2) for the triangle, O(n) for a single row",
    "space": "O(n^2) / O(n)"
  },
  "edgeCases": [
    "1-indexed vs 0-indexed rows",
    "n = 1",
    "Large r overflows fixed-width ints (not Python)"
  ],
  "commonMistakes": [
    "Computing factorials separately and overflowing",
    "Dividing with / instead of // and drifting into floats"
  ],
  "patternRecognition": [
    "Ask which of the three variants is wanted — the complexities differ"
  ],
  "followUps": [
    "Value at (r,c) in O(c)",
    "Row modulo a prime",
    "Connection to combinations in DP counting problems"
  ],
  "interviewInsight": "Clarify the variant first. Jumping straight to the full triangle when they wanted one row wastes the interview.",
  "related": [
    {
      "id": "mjritylmntn3tims",
      "title": "Majority Element (n/3 times)"
    },
    {
      "id": "3smprblm",
      "title": "3-Sum Problem"
    },
    {
      "id": "4smprblm",
      "title": "4-Sum Problem"
    }
  ]
}
