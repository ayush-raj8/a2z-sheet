{
  "id": "bbblsrt",
  "title": "Bubble Sort",
  "stepTitle": "Learn Important Sorting Techniques",
  "subStepTitle": "Sorting-I",
  "sourceUrl": "https://takeuforward.org/data-structure/bubble-sort-algorithm/",
  "intro": "Sorting is a primitive used inside harder solutions. Know stable vs unstable and the N log N comparison bound. Classic framing from the roadmap note “bubble sort algorithm”.",
  "intuition": "Unordered → ordered so later scans/binary search become possible.",
  "approach": [
    "Choose algorithm by constraints.",
    "Implement or call library sort.",
    "Use sorted order in the next step of the solution."
  ],
  "complexity": {
    "time": "O(n log n) typical",
    "space": "O(1)–O(n)"
  },
  "pitfalls": [
    "Unstable sort breaking equal-key order.",
    "Comparing ints with overflow (use long/comparator)."
  ],
  "languages": {
    "cpp": {
      "code": "#include <bits/stdc++.h>\nusing namespace std;\n// Teach-yourself scaffold for: Bubble Sort\nint main() {\n    ios::sync_with_stdio(false);\n    cin.tie(nullptr);\n    int n; \n    if (!(cin >> n)) return 0;\n    vector<int> a(n);\n    for (int i = 0; i < n; i++) cin >> a[i];\n    // TODO: core idea for \"Bubble Sort\"\n    // 1) brute force for correctness\n    // 2) replace with the pattern from this step\n    cout << a.size() << \"\\n\";\n    return 0;\n}",
      "notes": "Fill the TODO with the approach above; keep IO fast."
    },
    "java": {
      "code": "import java.util.*;\npublic class Main {\n    // Teach-yourself scaffold for: Bubble Sort\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        if (!sc.hasNextInt()) return;\n        int n = sc.nextInt();\n        int[] a = new int[n];\n        for (int i = 0; i < n; i++) a[i] = sc.nextInt();\n        // TODO: implement \"Bubble Sort\"\n        System.out.println(a.length);\n    }\n}",
      "notes": "Replace the TODO; prefer clear names over micro-opts first."
    },
    "python": {
      "code": "# Teach-yourself scaffold for: Bubble Sort\nimport sys\n\ndef solve(a):\n    # TODO: core idea for \"Bubble Sort\"\n    return len(a)\n\ndef main():\n    data = list(map(int, sys.stdin.buffer.read().split()))\n    if not data: return\n    n, *rest = data\n    a = rest[:n]\n    print(solve(a))\n\nif __name__ == \"__main__\":\n    main()",
      "notes": "PyPy helps some TLs; still aim for the right complexity."
    },
    "go": {
      "code": "package main\nimport (\n    \"bufio\"\n    \"fmt\"\n    \"os\"\n)\n// Teach-yourself scaffold for: Bubble Sort\nfunc solve(a []int) int {\n    // TODO: core idea for \"Bubble Sort\"\n    return len(a)\n}\nfunc main() {\n    in := bufio.NewReader(os.Stdin)\n    var n int\n    if _, err := fmt.Fscan(in, &n); err != nil { return }\n    a := make([]int, n)\n    for i := 0; i < n; i++ { fmt.Fscan(in, &a[i]) }\n    fmt.Println(solve(a))\n}",
      "notes": "Go rewards simple, explicit loops — good for interviews."
    }
  }
}
